# 작업 로그 — 2026-09-04

| 항목 | 결과 |
|---|---|
| 신규 발행 | hadesyi **416** 「vLLM 양자화 완전정리」 |
| 색인 요청 | **11건** (416 + hadesyi 10편) |
| hadesyi 300번대 | ✅ **46편 전부 처리 완료** |
| 남은 대상 | **116편** (1~275번대) |
| hadesyi 클릭 | **188회** (9/1 170 → 9/2 172 → **188**) |

---

## 1. 416 발행 — 유입 데이터가 고른 첫 원고

- URL: `https://hadesyi.tistory.com/416`
- 제목: **vLLM 양자화 완전정리 — GGUF·AWQ·GPTQ·W4A16·FP8 선택 기준**
- 카테고리: AI·LLM 엔지니어링
- 원문 10,971자 / 실제 렌더 **10,304자** / H2 14 / 표 11 / 코드블록 11 / 태그 9
- 발행 직후 **색인 요청 완료**
- 원고: `posts/hadesyi/10_vLLM-Quantization.md`

`vllm gguf` 계열 **129노출 / 9클릭**이 이 블로그 최대 유입원인데 양자화 포맷 비교 글이 공백이었던 자리입니다. **backlog를 유입 데이터로 재작성한 뒤 나온 첫 결과물**입니다.

주입은 **11블록 전부 통과, 전체 SHA-256 일치**, 전사 오류 0건. 진입 확인창도 뜨지 않았습니다.

---

## 2. 색인 요청 11건 — 300번대 소진

**✅ 요청 완료** — `416` · `321` · `316` · `314` · `312` · `310` · `309` · `307` · `299` · `286` · `280`

이로써 **8/30에 특정한 165편 중 300번 이상 46편이 전부 처리**됐습니다. 남은 **116편은 전부 1~275번대**입니다.

**다음 우선순위 10편:** `275·271·265·261·260·259·256·255·254·250`

### 9/2의 「할당량 초과」 해석이 맞았습니다

9/2에 `/321`이 두 번 연속 차단돼 **"차단된 URL을 바로 재시도하지 마라"** 고 적었는데, 오늘 **같은 `/321`이 한 번에 통과**했습니다.

> 💡 **`/321`이 특별했던 게 아니라 그날 할당량이 끝났던 것**입니다. 9/2에 "per-URL 차단일 수 있다"는 가능성도 함께 적었는데, **날짜가 바뀌자 바로 되는 것으로 그 가설은 정리됐습니다.** 차단은 **날짜 단위**입니다.

오늘은 11건에 차단이 없었습니다. 9/2(약 9건 후 차단)와 비교하면 **역시 날마다 다릅니다.**

---

## 3. 발행 절차 — 빈 필드에는 Ctrl+A 금지 (재확인)

9/2에 기록한 대로 **제목 필드가 비어 있을 때는 클릭 후 바로 타이핑**했고, 이번엔 `a` 혼입이 없었습니다. 절차가 정착됐습니다.

```
신규 글  →  클릭 → 바로 타이핑 → 값 검증
기존 글  →  클릭 → Ctrl+A → Delete → 타이핑 → 값 검증
```

---

## 4. 다음 할 일

1. **hadesyi 남은 116편** — `275·271·265·261·260·259·256·255·254·250` 부터. 차단되면 그날은 중단
2. **9/2 제목 수정 효과 확인** — `/98`·`/84`가 재크롤링돼 새 제목이 반영되는지, CTR이 오르는지. **최소 1~2주** 필요
3. **다음 원고** — hadesyi backlog 2순위 **「Kafka Consumer Lag 완전정리」**(28노출 0클릭, 순위 5.5~8.3) / ericreview는 **제목 점검이 우선**
4. **네이버 등록** — 🔒 사람 작업 대기
5. **2주 뒤 재측정** — 색인 증가(+35 확인됨)가 클릭 증가로 이어지는지
