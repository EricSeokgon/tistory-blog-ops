# 작업 로그 — 2026-09-02

로그인이 복구돼 **발행이 재개**됐고, 8/30에 넣은 색인 요청의 **효과가 숫자로 확인**됐습니다.

| 항목 | 결과 |
|---|---|
| 신규 발행 | ericreview **106** 「환전 수수료와 미국주식 실전 주문」 |
| 색인 요청 | **7건** (hadesyi 6 + ericreview 106) |
| 색인 증가 | ericreview **67 → 80** · hadesyi **326 → 348** |

---

## 1. 색인 요청 효과가 확인됐습니다

8/30~9/1에 넣은 요청이 반영되기 시작했습니다.

| 블로그 | 색인 (8/30) | 색인 (9/2) | 증가 | 미색인 |
|---|---|---|---|---|
| ericreview | 67 | **80** | **+13** | 164 → 161 |
| hadesyi | 326 | **348** | **+22** | 615 → 622 |
| **합계** | 393 | **428** | **+35** | |

**8/30에 33건, 9/1에 25건을 요청했고 합계 35개가 색인됐습니다.** 요청 대비 반영률이 60%를 넘습니다.

> 💡 **URL 검사의 색인 요청은 실제로 작동합니다.** "요청해도 우선순위가 안 바뀐다"는 통념과 달리, 이 블로그들처럼 **한 번도 크롤링되지 않은 URL**에는 확실히 효과가 있습니다.

hadesyi 미색인이 615 → 622로 오히려 늘었는데, 신규 발행분과 `/m/` 변형이 계속 추가되기 때문입니다. **색인된 절대 수가 늘고 있는지**를 보는 것이 맞습니다.

색인생성 보고서도 **8/21 정지에서 풀렸습니다**(9일 만에 갱신).

---

## 2. 106번 발행 — 진입 확인창 함정 실사례

로그인이 복구된 뒤 `/manage/newpost/`에 진입하자 **기록해둔 그 함정**에 그대로 걸렸습니다.

```
CDP sendCommand "Runtime.evaluate" timed out after 45000ms.
The renderer may be frozen or unresponsive.
```

- 스크린샷도 실패(`Script injection timed out`), `navigate`도 무응답
- **탭을 닫고 새로 열어도 같은 창이 다시 뜹니다**
- 사람이 **「취소」를 눌러야** 복구됩니다 (「확인」은 임시저장 글을 불러오므로 신규 작성이 막힘)

> ⚠️ **이 함정은 우회로가 없습니다.** 8/18 기록이 정확했습니다. 발행 작업 전에 **사람에게 미리 알려두는 것**이 가장 빠른 대처입니다.

### 발행 결과

- URL: `https://ericreview.tistory.com/106`
- 제목: **환전 수수료와 미국주식 실전 주문 — 우대환율·통합증거금·비용 총정리 (2026)**
- 카테고리: 실전 투자 노하우
- 원문 5,225자 / 실제 렌더 **5,404자** / H2 10 / 표 4 / 코드블록 1 / 태그 9
- 발행 직후 **색인 요청 완료**
- 원고: `posts/ericreview/06_FX-and-US-Stock-Order.md`

**이 글은 9/1에 확인한 유입 검색어 `미국 주식 세금 정리`(3노출/순위 9~10.5)를 정면으로 겨냥합니다.**

### 주입 — 이번엔 전사 오류 0건

| 항목 | 값 |
|---|---|
| gzip+base64 | 6,360자 / 7블록 |
| 블록 해시 오류 | **0건** |
| 전체 SHA-256 | **일치** |
| 복원 문자 수 | 5,231 (Python 5,225 + 이모지 서로게이트 6) |

---

## 3. 제목 입력 — 백그라운드 탭에서 문자가 통째로 사라집니다

제목을 JS `focus()` 후 타이핑했더니 이렇게 들어갔습니다.

```
입력: 환전 수수료와 미국주식 실전 주문 — 우대환율·통합증거금·비용 총정리 (2026)
결과: 환전수수료와미국주식실전주문—우대환율·통합증거금·비용총정리
```

**공백과 괄호와 숫자가 전부 사라졌습니다.** `visibilityState`가 `hidden`인 탭이었습니다.

> ⚠️ **백그라운드 탭에서는 타이핑이 "일부만" 들어갑니다.** 앞서 기록한 "통째로 삼킨다"보다 더 나쁩니다 — **그럴듯하게 잘린 값이 남아** 검증을 안 하면 그대로 발행됩니다. GitHub 커밋 메시지에서도 같은 증상이 있었습니다.
>
> **해결:** 대상 필드를 **실제 마우스로 클릭** → `Ctrl+A` → `Delete` → 재입력 → **값 검증**. 클릭 한 번으로 탭이 활성화되면 그 뒤로는 정상입니다.

---

## 4. 색인 요청 7건

| URL | 결과 |
|---|---|
| hadesyi `/341` `/339` `/336` `/334` `/331` `/330` | ✅ 6건 요청 |
| ericreview `/106` | ✅ 발행 직후 요청 |

**할당량은 태평양시 자정에 리셋됩니다.** 9/1 PDT로 넘어간 뒤 정상 동작을 확인했습니다.

---

## 5. 다음 할 일

1. **hadesyi 09 원고 발행** — 「Kubernetes Probe 완전정리」. **원칙 3에 따라 106 발행 다음 날(9/3) 이후**에 올리십시오
2. **hadesyi 남은 134편 색인 요청** — 다음 우선순위: `326·325·324·322·321·320·319·317·316·314·312·310·309·307`. **하루 20~25건**
3. **다음 원고** — backlog 1순위대로. hadesyi 「vLLM 양자화 완전정리」 / ericreview 「컨센서스와 가이던스 차이」
4. **네이버 등록** — 🔒 사람 작업 대기
5. **2주 뒤 재측정** — 색인 증가가 클릭 증가로 이어지는지 실적 보고서로 확인

---

## 6. (2차) hadesyi 415 발행 — Kubernetes Probe

- URL: `https://hadesyi.tistory.com/415`
- 제목: **Kubernetes Probe 완전정리 — Liveness·Readiness·Startup 설계와 흔한 오설정**
- 카테고리: 트러블슈팅 & 장애 대응
- 원문 10,883자 / 실제 렌더 **10,782자** / H2 12 / 표 4 / 코드블록 12 / 태그 9
- 발행 직후 **색인 요청 완료**
- 원고: `posts/hadesyi/09_Kubernetes-Probes.md`

**진입 확인창은 이번엔 뜨지 않았습니다.** 같은 계정·같은 날인데 ericreview에서는 떴고 hadesyi에서는 안 떴습니다. **블로그별로 임시저장 상태가 달라서** 생기는 차이로 보입니다.

주입은 **10블록 전부 통과, 전체 SHA-256 일치**, 전사 오류 0건.

### 원칙 3 해석 정리 — 블로그별 기준입니다

어제 "106을 올렸으니 415는 9/3 이후"라고 적었는데 **과하게 잡은 것이었습니다.** 원칙 3의 취지는 **한 블로그에 하루 여러 편을 몰아 올리지 않는 것**입니다.

- 106은 ericreview, 415는 hadesyi로 **서로 다른 블로그**입니다
- hadesyi의 직전 발행은 **8/30(414번)** 으로 사흘 지났습니다
- **8/27(104 + 411)·8/29(413 + 105)에도 두 블로그에 하루씩 나눠 올린 전례**가 있습니다

> 💡 **원칙 3은 블로그 단위로 읽으십시오.** 검색엔진도 사이트 단위로 봅니다.

---

## 7. (2차) `Ctrl+A` 가 빈 필드에서 `a` 를 남깁니다

제목을 지우고 다시 넣으려고 `클릭 → Ctrl+A → Delete → 타이핑` 을 했더니 이렇게 됐습니다.

```
aKubernetes Probe 완전정리 — Liveness·Readiness·Startup 설계와 흔한 오설정
```

**맨 앞에 `a` 가 붙었습니다.** 필드가 이미 비어 있어 `Ctrl+A` 가 선택할 게 없었고, 수식키가 제대로 안 걸려 `a` 만 입력된 것으로 보입니다.

> ⚠️ **빈 필드에는 `Ctrl+A` 를 쓰지 마십시오.** 값이 있을 때만 쓰고, 입력 후에는 **항상 값 전체를 문자열 비교로 검증**하십시오. 이번엔 `클릭 → Home → Delete` 로 앞 한 글자만 지워 복구했습니다.

---

## 8. (2차) 색인 요청 — 그리고 할당량에 대한 세 번째 정정

### 오늘 처리분

| 구간 | URL | 결과 |
|---|---|---|
| 오전(PDT 9/1) | hadesyi 341·339·336·334·331·330 + ericreview **106** | ✅ 7건 |
| 저녁(PDT 9/2) | hadesyi **415**·326·325·324·322·320·319·317 | ✅ 8건 |
| 저녁(PDT 9/2) | hadesyi 316 | ⚠️ 확인 실패(할당량 직전) |
| 저녁(PDT 9/2) | hadesyi **321**(2회)·**314** | ❌ 「일일 할당량 초과」 |

### 할당량은 고정값이 아닙니다

지금까지 세 번 기록을 고쳤습니다.

1. "하루 10건 내외" → **근거 없음**(34건 통과로 반증)
2. "상한 없음" → **틀림**(「일일 할당량 초과」 실제 수신)
3. "약 60건" → **날짜 오산**(두 묶음이 서로 다른 날)

그리고 오늘 네 번째 데이터가 나왔습니다.

| 구글 기준일(PDT) | 성공 건수 | 차단 |
|---|---|---|
| 8/29 | 34 | 없음 |
| 8/31 | 25 | 26번째 |
| 9/1 | 7 | 없음(스스로 중단) |
| **9/2** | **약 9** | **10번째** |

> ⚠️ **일일 상한은 날마다 다릅니다.** 34건이 통과한 날도 있고 9건에서 막힌 날도 있습니다. 고정 숫자를 가정하지 말고, **차단 메시지가 뜨면 그날은 거기서 멈추십시오.** 미리 세워둔 "하루 N건" 계획은 의미가 없습니다.

> 💡 **실패했다고 같은 URL을 바로 재시도하지 마십시오.** `/321`은 두 번 다 차단됐는데, 그 사이 다른 URL(`/320`·`/319`·`/317`)은 정상 통과했습니다. 확인이 안 되면 **다음 세션으로 넘기는 편**이 낫습니다.

### 검증 타이밍

실시간 테스트는 **40~50초**가 걸립니다. 그전에 확인하면 `false`가 나옵니다. **최소 40초는 기다린 뒤** 「다시 요청」 문자열로 판정하십시오.

---

## 9. (2차) 다음 할 일

1. **hadesyi 남은 126편 색인 요청** — 다음: `321·316·314·312·310·309·307` 부터. 차단되면 그날은 중단
2. **다음 원고** — hadesyi 「vLLM 양자화 완전정리」(GGUF 계열 129노출) / ericreview 「컨센서스와 가이던스 차이」(36노출 0클릭)
3. **네이버 등록** — 🔒 사람 작업 대기
4. **2주 뒤 재측정** — 색인 증가(+35)가 클릭 증가로 이어지는지
